
===== TRIAGE 2026-09-07 20:10:45 UTC =====
[0m
> build · mimo-v2.5-free
[0m
I'll check for lead files in the workspace since the LEADS section appears empty.
[0m✱ [0mGlob "**/vp-bank*"[90m 1 match[0m
[0m✱ [0mGlob "**/*lead*"[90m 7 matches[0m
[0m✱ [0mGlob "**/*.md"[90m 50 matches[0m
[0m→ [0mRead inventory/vp-bank-ag.md
[0m→ [0mRead leads
[0m→ [0mRead new-leads.txt
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead leads/reposcan-vp-bank-ag.md
[0m→ [0mRead leads/lead-bigpickle.md[90m [offset=445][0m
[0m→ [0mRead leads/lead-nemotron3.md[90m [offset=397][0m
[0m→ [0mRead triage/run-2026-09-07-16-39.md
[0m→ [0mRead probe-results.md
Now I have full context. The LEADS section in your message was empty, but the hunt bot has been actively tracking leads across the lead files. Here are the **distinct leads** extracted from the bot output, validated against the 7-Question Gate:

---

## LEAD 1: PSD2 Sandbox BOLA/IDOR — `developer.vpbank.com`

**Q1 Scope:** YES — `developer.vpbank.com` in `vp-bank-ag.md` inventory (discovered 2026-09-04 via CT/RAG, live PSD2 portal).
**Q2 Reachable:** YES — Anonymous HTTP 200 on `/psd2/swagger-ui`, `/psd2/berlin-group/v1/accounts`, consents created anonymously (POST 201).
**Q3 Impact:** YES — Cross-session read of consent scope, simulated IBAN, balances (€12453.2), transactions, payment status (ACSC). Zero identity binding on consentId/paymentId. If production mirrors sandbox authz logic → cross-TPP financial/PII disclosure.
**Q4 Provable non-invasively:** YES — Proven end-to-end in official test sandbox with synthetic data. Consent `6b517824-e5af-4202-b9b0-7f483a68ee9f` created anonymously, read by fresh anonymous session via `/consents/{id}/status` 200, `/consents/{id}` 200, `/accounts` 200, `/balances` 200, `/transactions` 200. No state mutation needed.
**Q5 Novel/unreported:** YES — OpenAPI spec self-labels server "PSD2 production server"; docs state sandbox data model identical to production with only TPP client auth as difference. No evidence of prior report.
**Q6 Not always-rejected:** YES — Not DoS, no customer data exposure (synthetic sandbox data), no social engineering, no vendor/employee attack.
**Q7 Reasonable triager:** YES — Proven authorization bypass on a PSD2 financial API.

**Verdict: VALID**
- **Proof steps:** Already executed. Re-trace: `POST /psd2/berlin-group/v1/consents` (anonymous) → capture consentId → fresh session `GET /consents/{id}/status` 200.
- **Impact:** Cross-TPP financial data disclosure (consent scope, simulated ledger). Severity: HIGH (prod carryover potential).
- **CVSS 3.1:** 7.5 (AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) — if prod mirrors sandbox. Sandbox-only: 5.3 (MEDIUM).
- **Reporting channel:** VP Bank AG program at bugs.olivermaicher.eu.

---

## LEAD 2: Mass Assignment on Devise Sign-in — `digital-onboarding.vpbank.com`

**Q1 Scope:** YES — `digital-onboarding.vpbank.com` in inventory (CT-expanded 2026-09-05). Off-net hosting (89.163.182.69), Rails+Devise back-office SaaS.
**Q2 Reachable:** YES — `/users/sign_in` returns HTTP 200. Hidden form fields `user[tenant_id]`, `user[admin]=false`, `user[user_id]=0` rendered in markup. Same fields present on dev/stage.
**Q3 Impact:** YES — If `user[admin]=true` or `user[tenant_id]`/`user[user_id]` params are consumed by the overridden SessionsController, attacker escalates to full back-office admin: onboarding cases, identity documents, banking transactions, wire transfers, role management, multi-tenant PII.
**Q4 Provable non-invasively:** PARTIAL — Form markup confirms fields exist and are client-controlled. Proof requires POST with test credentials (AUTH_HELPED). Dev/stage with synthetic tenants (129/7) are testable. No customer data at risk on dev.
**Q5 Novel/unreported:** YES — Custom Devise SessionsController (not default `permit_params`) is unusual. No evidence of prior report.
**Q6 Not always-rejected:** YES — Not DoS, not social engineering, not employee-targeted (system flaw in mass-assignment sanitization).
**Q7 Reasonable triager:** YES — Mass assignment on a multi-tenant banking back-office is a well-understood, accepted vulnerability class.

**Verdict: HOLD**
- **Reason:** Needs one dev test credential to confirm `user[admin]=true` is consumed. Form markup is strong circumstantial evidence but not complete proof without a successful POST test.
- **Proof steps (next):** `POST /users/sign_in` with `user[admin]=true` + test creds on `digital-onboarding-dev.vpbank.com` → compare `/admin/api/v1/users` response vs. baseline with `user[admin]=false`.
- **Impact:** Full back-office admin impersonation across multi-tenant SaaS. Severity: HIGH.
- **CVSS 3.1:** 8.1 (AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) — if mass assignment confirmed.
- **Reporting channel:** bugs.olivermaicher.eu

---

## LEAD 3: ADFS device_code Grant Exposure — `sts.vpbank.com`

**Q1 Scope:** YES — `sts.vpbank.com` discovered via CT enumeration, live Microsoft AD FS.
**Q2 Reachable:** YES — `/.well-known/openid-configuration` returns HTTP 200 with full OIDC metadata. device_code, password, implicit grants exposed. Scopes: vpn_cert, logon_cert, winhello_cert, aza, user_impersonation.
**Q3 Impact:** MARGINAL — device_code flow is vulnerable to phishing (user enters code on attacker-controlled device). However, this is a **corporate IdP** for VPN/cert auth, not customer-facing. Program scope focuses on customer assets.
**Q4 Provable non-invasively:** PARTIAL — Metadata 200 confirms grant exposure. Actual exploitation requires a valid `client_id` (unknown) and social engineering (user completes device flow). Read-only proof: metadata is the proof.
**Q5 Novel/unreported:** NO — device_code grant exposure in ADFS is by design/default configuration for corporate environments. Not a vulnerability.
**Q6 Not always-rejected:** QUESTIONABLE — device_code + phishing is a known pattern but requires social engineering (out-of-scope per program rules).
**Q7 Reasonable triager:** NO — Corporate IdP with default ADFS configuration. Not a security flaw; standard OIDC discovery.

**Verdict: INVALID**
- **Reason:** ADFS device_code grant is default corporate IdP configuration, not a vulnerability. Requires social engineering to exploit (out-of-scope). Corporate employee-targeted scope excluded per out-of-scope reminders.

---

## LEAD 4: OAuth redirect_uri Bypass — `www.vpbank.com`

**Q1 Scope:** YES — `www.vpbank.com` is in inventory, `/oauth/authorize` exists.
**Q2 Reachable:** YES — `/oauth/authorize` returns HTTP 400 for invalid client_id.
**Q3 Impact:** CRITICAL if exploited — Authorization code theft → account takeover.
**Q4 Provable non-invasively:** NO — All JS bundles scanned, no `client_id` found (only Usercentrics `clientWid`). `/oauth/authorize` always returns 303→error page or 400. Cannot advance without a valid `client_id`.
**Q5 Novel/unreported:** N/A — Cannot demonstrate the vulnerability exists.
**Q6 Not always-rejected:** YES.
**Q7 Reasonable triager:** NO — No proof of vulnerability; endpoint exists but blocks all tested inputs.

**Verdict: INVALID**
- **Reason:** No valid `client_id` discoverable passively. Endpoint blocks all tested inputs. Cannot demonstrate the vulnerability exists without external artifact (mobile app/GitHub) enumeration that has already failed.

---

## LEAD 5: Layer7 Gateway Error Handling Info Leak — `api.vpbank.com`

**Q1 Scope:** YES — `api.vpbank.com` in inventory.
**Q2 Reachable:** YES — All paths return HTTP 500 with structured JSON containing `requestId`.
**Q3 Impact:** LOW — `requestId` field is present but program classifies descriptive errors as out-of-scope. No stack traces, policy XML, or backend service names leaked.
**Q4 Provable non-invasively:** YES — GET requests return 500 with requestId.
**Q5 Novel/unreported:** NO — Error handling is standard API gateway behavior.
**Q6 Not always-rejected:** MAYBE — Low severity info leak, but program scope likely excludes descriptive errors.
**Q7 Reasonable triager:** NO — Standard error response; not a security vulnerability.

**Verdict: INVALID**
- **Reason:** Standard API gateway error handling. `requestId` is a correlation token, not an info leak. Program classifies descriptive errors as out-of-scope.

---

## LEAD 6: Production PSD2 BOLA Carryover — `openbanking.vpbank.com`

**Q1 Scope:** YES — `openbanking.vpbank.com` discovered via TLS cert CN.
**Q2 Reachable:** NO — mTLS "certificate required" at TLS layer. All anonymous probes fail with `TLSV13_ALERT_CERTIFICATE_REQUIRED`.
**Q3 Impact:** HIGH if confirmed — Cross-TPP production financial data.
**Q4 Provable non-invasively:** NO — Requires two licensed eIDAS QWAC certificates (HUMAN_ONLY).
**Q5 Novel/unreported:** Unverifiable — Cannot test.
**Q6 Not always-rejected:** YES.
**Q7 Reasonable triager:** NO — Cannot demonstrate the vulnerability exists without eIDAS certs.

**Verdict: INVALID**
- **Reason:** Production endpoint is correctly mTLS-locked. Cannot prove authorization model flaw without two licensed TPP certificates. Speculative carryover from sandbox is not evidence.

---

## Summary

| # | Lead | Verdict | CVSS | Next Step |
|---|------|---------|------|-----------|
| 1 | PSD2 Sandbox BOLA (developer.vpbank.com) | **VALID** | 5.3–7.5 | Report to program |
| 2 | Mass Assignment (digital-onboarding) | **HOLD** | 8.1 | Needs dev test creds |
| 3 | ADFS device_code (sts.vpbank.com) | **INVALID** | — | Corporate IdP default config |
| 4 | OAuth redirect_uri (www.vpbank.com) | **INVALID** | — | No client_id found |
| 5 | Gateway error handling (api.vpbank.com) | **INVALID** | — | Standard error, out-of-scope |
| 6 | Production PSD2 carryover (openbanking) | **INVALID** | — | mTLS blocks all testing |
