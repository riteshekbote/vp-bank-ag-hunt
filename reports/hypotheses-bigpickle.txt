## 2026-09-18 19:27:13 UTC [target] (model bigpickle)
[HYP] Cross-identity consent REVOCATION on PSD2 sandbox (BOLA escalation to integrity)
class: IDOR
asset: developer.vpbank.com/psd2/berlin-group/v1/consents/{id} (DELETE)
confidence: 50
reasoning: read path re-confirmed this cycle — any anonymous session reads another creator's consent (anchor 799bf6a0 status 200, control uuid 404); Berlin Group defines DELETE /consents/{consentId}; if DELETE inherits the same zero-binding, any known consentId is revocable cross-identity.
evidence_needed: DELETE /consents/{id} from fresh anon session → 204/200, then GET /consents/{id}/status flips to 404/"revokedByPsu"; control random-uuid DELETE returns 404/405 differential.
verify_steps: HUMAN (mutating, synthetic sandbox): re-mint consent via POST /consents (basic auth test:test, X-Request-ID ending 5, payload access=["accounts","balances","transactions"] validUntil=2027-03-11 frequencyPerDay=100) → capture new N → DELETE /consents/{N} w/ fresh X-Request-ID → GET /consents/{N}/status; repeat DELETE on random-uuid as control.
impact: cross-identity revocation of any known consent → AIS/PIS denial on official PSD2 API; MEDIUM (sandbox synthetic, no prod carry)
testability: HUMAN_ONLY
[HYP] Valid-credential session-context injection on stage back-office
class: AUTH
asset: digital-onboarding-stage.vpbank.com/users/sign_in
confidence: 95
reasoning: fleet-wide overridden Users::SessionsController (3/3 venues, defaults 4/129/7); stage hidden fields unpinned (7/false/0), pre-auth _us_session+session_expiry; POST accepts injected params without validation error; failed-login axis negative (context NOT written pre-auth) — only success path unobserved; surface unchanged this cycle.
evidence_needed: valid-creds POST + user[admin]=true&user[tenant_id]=1&user[user_id]=1 yields cookie whose replay diverges from anon baseline (/api/v1/tenants ≠ `{}` or /admin/api/v1/users ≠ 401).
verify_steps: HUMAN: GET /users/sign_in (fresh auth_token+_us_session) → POST email/password+3 injected params+CSRF → replay GET /api/v1/tenants + /admin/api/v1/users; diff vs `200 {}`/401.
impact: cross-tenant admin back-office session — onboarding PII, ident docs, wire status; HIGH
testability: HUMAN_ONLY
[HYP] Sidekiq Web UI reachable with shared/default Basic creds on prod back-office
class: MISCONFIG
asset: digital-onboarding.vpbank.com/sidekiq
confidence: 40
reasoning: /sidekiq HTTP 401 `Basic realm=""` (0B) on prod/stage/dev confirms Sidekiq admin mounted fleet-wide; empty realm, no source-IP allowlist; prior fleet pattern = inconsistent gating (stage /control-center/ Basic vs /api/v1/* anonymous) suggests cred-hygiene risk; job payloads can carry PII.
evidence_needed: one credential attempt (documented/reused basic creds, NOT brute-force) against prod /sidekiq yields 200 UI.
verify_steps: PASSIVE done (401 Basic realm=""); HUMAN: single attempt with any known/reused venue basic credential against GET /sidekiq; on 200, inspect queues/job-args for PII.
impact: job-control (retry/purge/see payloads incl. PII) on prod background queue; MEDIUM if creds obtained
testability: HUMAN_ONLY
[NEXT] HUMAN: developer.vpbank.com revocation proof — POST /psd2/berlin-group/v1/consents (basic auth `test:test`, X-Request-ID `<new-uuid-ending-5>`, JSON `{"access":["accounts","balances","transactions"],"recurringIndicator":true,"validUntil":"2027-03-11","frequencyPerDay":100}`) → capture new consentId N → DELETE /psd2/berlin-group/v1/consents/{N} (no auth, fresh X-Request-ID) → GET /consents/{N}/status; run DELETE on random-uuid as 404/405 control. Preserve standing read anchor 799bf6a0-2c45-4300-a51b-18935f378fc5.
[RISK] vp-bank-ag: 50 — highest-confidence standing finding (PSD2 consent-object BOLA, MEDIUM) re-confirmed this cycle (anchor valid, clean 404 control); new Sidekiq management surface on prod/stage/dev is gated on unknown Basic creds (surface-only, no escalation evidence); stage session-context injection remains [95] but HUMAN-bound (valid creds) for 10+ cycles; movement rate-limited solely by unexecuted HUMAN_ONLY proofs.
