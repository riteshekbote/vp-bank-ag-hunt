## 2026-09-13 18:30:46 UTC [target] (model bigpickle)
[HYP] Sandbox consent re-mint restores cross-session BOLA fixture (read service confirmed healthy)
class: IDOR
asset: developer.vpbank.com/psd2/berlin-group/v1/consents
confidence: 60
reasoning: BOLA proven end-to-end pre-churn (anon POST 201 → 200 cross-session reads, zero binding); this cycle consent-reads return clean 404 anon+basic for unknown ids and /accounts 200 [] — read path healthy, no 500 degradation remaining; same anonymous POST mint path that worked 09-04/09-05
evidence_needed: fresh POST mint returns consent id readable by a different anonymous session via /consents/{id}/status, /accounts, /balances
verify_steps: HUMAN (sandbox synthetic, in-program): POST /psd2/berlin-group/v1/consents {"access":["accounts","balances","transactions"],"recurringIndicator":true,"validUntil":"2027-03-11","frequencyPerDay":100} + X-Request-ID:<uuid> + Content-Type:application/json; capture 201 id; GET /consents/{id}/status + /accounts + /balances from fresh anonymous session
impact: restores live fixture for standing CRITICAL-class financial-API BOLA finding (synthetic, no prod data); re-establishes reportable POC
testability: HUMAN_ONLY
[HYP] Valid-credential sign_in persists injected admin/tenant session-context on stage (success-path never observed)
class: AUTH
asset: digital-onboarding-stage.vpbank.com/users/sign_in
confidence: 50
reasoning: fleet-wide overridden Users::SessionsController (3/3, defaults 4/129/7); failed-login path NEGATIVE 09-10 (context NOT written pre-auth); form live this cycle (200/23725B) with unpinned hidden user[tenant_id]=7; success-path behavior never observed
evidence_needed: valid-creds POST yields cookie whose replay returns populated /api/v1/tenants (baseline `{}`) or 200 on /admin/api/v1/users (baseline 401)
verify_steps: HUMAN: GET /users/sign_in capture authenticity_token + _us_session; POST email+password + user[admin]=true&user[tenant_id]=1&user[user_id]=1 + CSRF; follow redirect; replay GET /api/v1/tenants and /admin/api/v1/users; diff vs baseline
impact: cross-tenant admin back-office session — onboarding PII, ident docs, wire status; HIGH
testability: HUMAN_ONLY
[HYP] PSD2 sandbox BOLA carries to production consent/account objects via shared handler
class: IDOR
asset: openbanking.vpbank.com/psd2/berlin-group/v1/consents/{id}
confidence: 45
reasoning: mechanism prior-proven; prod shares Berlin Group v1 surface and CN=openbanking.vpbank.com; spec self-labels server "PSD2 production server"; only mTLS (QWAC) separates; spec confirms no OAuth layer
evidence_needed: credentialed 200-vs-404 differential on non-owned consent/payment id against production
verify_steps: HUMAN: with any valid QWAC GET /psd2/berlin-group/v1/consents/{foreign-id}/status and /accounts → 200 = prod BOLA confirmed
impact: cross-TPP read of consent/account/balance/payment objects on production ASPSP; CRITICAL
testability: HUMAN_ONLY
[NEXT] HUMAN: POST `https://developer.vpbank.com/psd2/berlin-group/v1/consents` body `{"access":["accounts","balances","transactions"],"recurringIndicator":true,"validUntil":"2027-03-11","frequencyPerDay":100}` + `X-Request-ID: <uuid>` + `Content-Type: application/json`; on HTTP 201 capture `consentId`, then from a fresh anonymous session GET `/consents/{id}/status`, `/accounts`, `/balances` — non-404/200 cross-session reads re-establish the standing sandbox BOLA fixture (read path verified healthy this cycle, only the expired anchor blocks).
[RISK] vp-bank-ag: 74 — Approved sandbox BOLA (financial API, zero binding on consentId/paymentId) fixture restorable with one in-program POST and the read path is confirmed healthy; fleet-wide Devise session-context injection live at 3/3 venues (success-path unproven, needs creds); production carryover plausible but mTLS-gated (HUMAN_ONLY). Residual surface unchanged-negative: api/www/dev-stage/sts/mobile/ebics/tracking/beta/concentsol exhausted or gated; sandbox payment/secure_session BOLA axes dead.
