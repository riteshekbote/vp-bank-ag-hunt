## 2026-09-18 12:40:11 UTC [target] (model bigpickle)
[HYP] Cross-identity consent REVOCATION on PSD2 sandbox (BOLA escalation to integrity)
class: IDOR
asset: developer.vpbank.com/psd2/berlin-group/v1/consents/{id} (DELETE)
confidence: 50
reasoning: Berlin Group spec defines DELETE /consents/{consentId} (revocation); GET/status on this sandbox are proven zero-binding (any anonymous session reads another TPP's consent — 4/4 sessions). If DELETE inherits the same binding gap, any known consentId can be revoked cross-identity, killing a TPP's active payment/account consent — escalation from passive read to destructive integrity.
evidence_needed: DELETE /consents/799bf6a0… from fresh anonymous session returns 204/200, then GET /consents/{id}/status flips to 404/"revokedByPsu", while control random-uuid DELETE returns 404/405 differential.
verify_steps: HUMAN (mutating): GET /consents/799bf6a0…/status (baseline valid) → DELETE /consents/799bf6a0… w/ X-Request-ID — then GET status again; compare with DELETE on random-uuid.
impact: cross-identity revocation of any known consent → payment/account-consent denial on an official PSD2 API; sandbox-level POC of broken authz model; MEDIUM (synthetic data, no prod carry)
testability: HUMAN_ONLY
[HYP] Valid-credential session-context injection on stage back-office
class: AUTH
asset: digital-onboarding-stage.vpbank.com/users/sign_in
confidence: 95
reasoning: fleet-wide overridden Users::SessionsController (3/3 venues, defaults 4/129/7); stage hidden fields unpinned (7/false/0), 3× auth_token, pre-auth _us_session; failed-login axis negative (context NOT written pre-auth); success path unobserved; POST accepts injected params without validation error; sign_in live 200 this cycle.
evidence_needed: valid-creds POST + user[admin]=true&user[tenant_id]=1&user[user_id]=1 yields cookie whose replay diverges from anon baseline (/api/v1/tenants ≠ `{}` or /admin/api/v1/users ≠ 401)
verify_steps: HUMAN: GET /users/sign_in (fresh token) → POST email/password + 3 injected params + CSRF + _us_session → replay GET /api/v1/tenants + /admin/api/v1/users; diff vs `200 {}`/401
impact: cross-tenant admin back-office session — onboarding PII, ident docs, wire status; HIGH
testability: HUMAN_ONLY
[HYP] Anonymous consent mint still possible → fully credential-free BOLA chain
class: IDOR
asset: developer.vpbank.com/psd2/berlin-group/v1/consents
confidence: 45
reasoning: 09-05 anonymous POST → 201 (chain fully anon then); 09-17 re-mint used basic auth test:test; no code/service signal that anonymous mint was disabled; if live, mint→cross-read becomes credential-free end-to-end (cleanest reportable PoC)
evidence_needed: anonymous POST /consents (no Authorization) → 201 new consentId; fresh anon session /status 200
verify_steps: HUMAN: POST /psd2/berlin-group/v1/consents JSON {"access":["accounts","balances","transactions"],"recurringIndicator":true,"validUntil":"2027-03-11","frequencyPerDay":100} + X-Request-ID:<new uuid> + Content-Type:application/json, NO auth header → capture consentId → anon GET /consents/{id}/status
impact: removes "attacker identity at mint" caveat from the BOLA report; MEDIUM (sandbox, synthetic data)
testability: HUMAN_ONLY
[NEXT] HUMAN: stage valid-creds POST — GET https://digital-onboarding-stage.vpbank.com/users/sign_in (fresh token + _us_session), POST `email/<valid_creds>`+`password` + `authenticity_token` + `user[email]&user[password]&user[tenant_id]=1&user[admin]=true&user[user_id]=1`; on success replay GET /api/v1/tenants and /admin/api/v1/users; diff vs anon `200 {}`/`401` baseline. If still blocked, escalate to DELETE /psd2/berlin-group/v1/consents/799bf6a0-2c45-4300-a51b-18935f378fc5 revocation test.
[RISK] vp-bank-ag: 50 — highest-confidence standing finding (PSD2 consent-object BOLA) independently re-confirmed across a 4th fresh anonymous session with clean 404 control; stage session-context injection surface still live at 95-confidence but Human-bound (valid creds) for 10+ cycles; discovery gap closed — further movement rate-limited solely by unexecuted HUMAN_ONLY proofs (valid-creds POST, consent re-mint/DELETE), not by surface.
